SSI_CONSUMER_ID=your_consumer_id_here
SSI_CONSUMER_SECRET=your_consumer_secret_here

# Optional: if you already generated a token, the fetch script can use it
# directly instead of calling Market/AccessToken.
# SSI_ACCESS_TOKEN=your_access_token_here
