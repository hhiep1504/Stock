# Dataset

This folder keeps the compact sample dataset used by the default experiment:

- `stock_market_19_24.csv`: daily stock-price input.
- `min_max_return.csv`: min/max return targets.

Larger raw and derived market-data files are ignored by git. See
`docs/DATA.md` for the data policy and regeneration notes.
